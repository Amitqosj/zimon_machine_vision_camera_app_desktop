# Base URL of the FastAPI service (no trailing slash). Build-time for Vite.
# Local dev: copy to .env.development or use defaults below.
VITE_API_URL=http://127.0.0.1:8010
# Deployed (Render): API = https://zimon-machine-vision-camera-backend.onrender.com
